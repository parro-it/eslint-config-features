{
  "name": "eslint-config-features",
  "version": "1.0.2",
  "description": "Automatically configure eslint ecmaFeatures base on your package.json `engines` field",
  "repository": "parro-it/eslint-config-features",
  "license": "MIT",
  "author": "parro-it",
  "scripts": {
    "test": "eslint . && node test | faucet",
    "lint": "eslint ."
  },
  "keywords": ["eslint", "config", "ecmaFeatures", "features", "automatically"],
  "engines": {
    "node": ">=0.10"
  },
  "eslintConfig": {
    "extends": "defaults/configurations/gulp"
  },
  "devDependencies": {
    "eslint": "^1.10.3",
    "eslint-config-defaults": "^8.0.2",
    "faucet": "0.0.1",
    "tape": "^4.2.2"
  },
  "files": [
    "index.js"
  ],
  "dependencies": {
    "arr-flatten": "^1.0.1",
    "auto-babel": "^0.4.0",
    "babelplugin-to-eslintrule": "^1.0.1",
    "filter-obj": "^1.1.0",
    "map-obj": "^1.0.1",
    "object-values": "^1.0.0",
    "semver-first-satisfied": "^1.0.0"
  }
}
